group("group-1", () => {
    test("1 should be equal to 1", 1).toBe(2);
    test("3 should be equal to 3", 3).toBe(2);
    test("3 should be equal to 3", 3).toBe(3);
    test("3 should be equal to 3", 3).toBe(3);
});